{
  "name": "letterchain",
  "version": "2.0.0",
  "description": "LetterChain — Decentralized letter of recommendation system with seeker/sponsor roles and on-chain verification",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "verify": "hardhat verify --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  }
}
